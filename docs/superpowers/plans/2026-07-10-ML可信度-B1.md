# ML 可信度（B1·修数据泄露）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans。Steps use checkbox (`- [ ]`)。

**Goal:** 修复 ML 选股的数据泄露/未来函数，让离线指标反映真实泛化能力（修复后 AUC 下降到真实区间 = 成功）。

**Architecture:** 改 `feature_engineering.py`（去 bfill + 标签 valid_mask + 特征平稳化 + 存 date）+ `model_trainer.py`（按日期切分 + scaler 隔离 + RF 参数）→ 重新生成训练数据 + 训练 + AUC 对比验证。

**Spec:** `docs/superpowers/specs/2026-07-10-ML可信度-B1-design.md`
**提交策略:** B1 完成后一个 commit `fix: 修复 ML 数据泄露`。AUC 下降是预期。

---

## File Structure

| 文件 | 改动 |
|------|------|
| `src/data/feature_engineering.py` | `:248` 去 bfill；`:490-513` 标签 valid_mask 过滤；`:536` 存 dates_npy；`:548-569` 特征求 close/OBV |
| `src/data/model_trainer.py` | `train()` 加 dates 参数 + 按日期切分；preprocess 重构（scaler 仅训练集 fit）；`:293-302` RF 参数收紧；`train_models()` 加载 dates_npy |

---

## Task 1: feature_engineering — 去 bfill

**Files:** Modify `src/data/feature_engineering.py:248`

- [ ] **Step 1:** 将 `result = result.fillna(method="bfill").fillna(0)` 改为 `result = result.fillna(0)`
- [ ] **Step 2:** 验证 `grep -n "bfill" src/data/feature_engineering.py` 无输出

## Task 2: feature_engineering — 标签 valid_mask + 存 date

**Files:** Modify `src/data/feature_engineering.py:489-513, 536`

- [ ] **Step 1:** 执行时先读 `generate_training_data` 的 `all_features/all_labels/all_info` 初始化与 stock 循环结构（约 440-488），确认插入点
- [ ] **Step 2:** 用 `max_horizon` 构造一次 `valid_mask = (close.shift(-max_horizon)/close-1).notna()`（末尾 max_horizon 行为 False）
- [ ] **Step 3:** 标签生成（490-501）改为对每个 horizon 用其自身 `valid_mask_h = future_returns_h.notna()` 过滤后 extend；追加循环（508-513）改为仅追加 max_horizon valid_mask 为真的 features/info（每 stock 一份特征，配最长 horizon 的 mask）
- [ ] **Step 4:** 保存处（536 附近）新增 `np.save(os.path.join(output_dir, f"dates_{timestamp}.npy"), np.array([i["date"] for i in all_info]))`
- [ ] **Step 5:** 验证：样本数 < 修复前；`dates_*.npy` 存在

## Task 3: feature_engineering — 特征求 close/OBV

**Files:** Modify `src/data/feature_engineering.py:548-569`

- [ ] **Step 1:** `_select_feature_columns` 的 `feature_patterns` 删除 `"close"` 和 `"OBV"` 两项
- [ ] **Step 2:** 验证特征列数减少 2

## Task 4: model_trainer — 时序切分 + scaler 隔离

**Files:** Modify `src/data/model_trainer.py`（train + preprocess_data + train_models）

- [ ] **Step 1:** 执行时读 `train_models`（加载 npy + 调 train），确认加载逻辑
- [ ] **Step 2:** `train()` 签名加 `dates: Optional[np.ndarray] = None`
- [ ] **Step 3:** `train()` 切分：dates 提供则按日期排序，前 `(1-test_size)` 为 train、后为 test（不打乱）；否则 fallback 旧 split + warning
- [ ] **Step 4:** preprocess 重构：切分在前，scaler 仅 `fit(X_train)` 后 transform 双方
- [ ] **Step 5:** `train_models()` 加载 `dates_*.npy` 传给 `train(dates=...)`
- [ ] **Step 6:** 验证：日志显示按日期切分

## Task 5: model_trainer — RF 参数收紧

**Files:** Modify `src/data/model_trainer.py:293-302`

- [ ] **Step 1:** `model_params`: `max_depth: 20→12`，`min_samples_leaf: 1→50`，`min_samples_split: 2→100`

## Task 6: 重新生成训练数据 + 训练 + AUC 对比

- [ ] **Step 1:** 记录修复前 AUC（旧模型评估或日志）
- [ ] **Step 2:** 重新生成：`conda run -n ashare-llm-analyst python -c "from src.data.feature_engineering import MLTrainingDataGenerator; MLTrainingDataGenerator().generate_training_data('training_data')"`
- [ ] **Step 3:** 重新训练：`conda run -n ashare-llm-analyst python -c "from src.data.model_trainer import RandomForestTrainer; RandomForestTrainer().train_models('return_5d_gt_5pct')"`
- [ ] **Step 4:** 对比测试集 AUC：预期虚高(~0.7+)→0.52-0.58。**下降即成功**

## Task 7: 提交

- [ ] `git add src/data/feature_engineering.py src/data/model_trainer.py docs/superpowers/`
- [ ] `git commit -m "fix: 修复 ML 数据泄露（时序切分+去bfill+标签valid_mask+特征平稳化）"` + body 说明 AUC 下降是修复成功标志

---

## 风险与注意
- **AUC 暴跌是成功标志**：commit message 务必说明
- **多 horizon valid_mask**：用 max_horizon 一次性过滤避免错位
- **训练数据生成耗时**：Step 2 可能数分钟；过慢可缩小股票范围
- **管道兼容**：旧 npy 无 dates 时 fallback + warning
